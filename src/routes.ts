import { Application } from "express";
import { RouteConstants } from "./constants/app.constant";

export default class Routes {

    private api = RouteConstants.ROUTE_API;

    constructor(app: Application) {
        // create & update route

        // get route

        // delete route
    }
}
